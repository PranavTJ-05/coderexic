{
  "name": "@coderexic/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsx watch --conditions=source --env-file-if-exists=../../.env src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "db:migrate": "tsx --conditions=source --env-file-if-exists=../../.env src/migrate.ts",
    "github:smoke": "tsx --conditions=source --env-file-if-exists=../../.env src/scripts/github-smoke.ts"
  },
  "dependencies": {
    "@coderexic/core": "workspace:*",
    "fastify": "^5.12.5",
    "zod": "^4.6.5",
    "bullmq": "^6.3.8"
  }
}
